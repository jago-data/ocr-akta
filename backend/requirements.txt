# OCR Akta backend — same convention as osg-prod: >= for libraries, == only when
# a pin has a written reason next to it.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9   # FastAPI refuses a File() route without it

httpx>=0.27.0             # client for the internal OCR API (and the test client)
pymupdf>=1.24             # PDF page counting / upload sanity checks

ldap3>=2.9.1              # directory auth in production; dev login needs nothing

# test-only (small, offline)
pytest>=8.0.0
